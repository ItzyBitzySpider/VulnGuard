rules:
  - id: test-regex-or
    message: 'regex OR expression'
    severity: WARNING
    case_sensitive: true
    fix: 'AAAA'
    regex:
      - regex_or:
        - regex: '\bdb.many\b'
        - regex: '\bdb.one\b'