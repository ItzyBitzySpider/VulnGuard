rules:
  - id: test-regex-or
    message: 'regex OR expression'
    severity: WARNING
    reference: "https://www.bing.com"
    case_sensitive: true
    regex:
      - regex_or:
        - regex: '\bdb.many\b'
        - regex: '\bdb.one\b'